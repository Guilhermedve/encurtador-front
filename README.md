This is the frontend of a URL shortener application.
It provides a simple interface where users can submit a long URL and receive a shortened version generated by the backend service.

Este é o frontend de uma aplicação de encurtador de URLs.
Ele fornece uma interface simples onde o usuário pode inserir uma URL longa e receber uma versão encurtada gerada pelo backend.
